#ifndef CHOOSE_H
    #define CHOOSE_H
    #include <stdio.h>
   
#define char *compression = compression
#define char *decompression = decompression

/*!
     * @brief this function allows the user to choose the operating mode of the program(compression/decompression).
     * @param choice operating mode choosed by the user.
     * @return  integer value used in the main to choose the operating mode.
     */
int choose(char **choice);

#endif